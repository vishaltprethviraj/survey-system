import { SurveyResponse } from './survey-response.model';

export class ReportFinal {
    public surveyresponse:SurveyResponse[]
    constructor(surveyresponse:SurveyResponse[]) {
        this.surveyresponse;
    }
}